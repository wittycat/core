package com.core.algorithm.kaoshi202205.四堆栈队列;

/**
 *
 *
 * 1.Stack
 *
 * 2.
 *
 * 3.PriorityQueue<Integer> minHeap = new PriorityQueue<Integer>(); //⼩顶堆，默认容量为11
 *   PriorityQueue<Integer> maxHeap = new PriorityQueue<Integer>(11,new Comparator<Integer>(){ //⼤顶堆，容量11
 *         @Override
 *         public int compare(Integer i1,Integer i2){
 *             return i2-i1;
 *         }
 *     });
 *
 */