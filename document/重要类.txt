1.反射
    获取对象字节对象的3中方式
   		Class class1 = 对象.class;
		Class<?> class2 = this.getClass();
		Class.forName("")
2.动态代理
  jdk:接口InvocationHandler  实现invoke
  cglib：接口MethodInterceptor 实现intercept   创建Enhancer设置父接口
3.jdbc
  Statement的子类   
  PreparedStatement:Statement的子类 预编译
  ResultSet:结果集 getGeneratedKeys
  Statement的generatedKeys()方法返回ResultSet 可以获取主键
4.多线程
     开启线程方式：Thread，Callable，Runnable
     同步工具类主要包括
	  闭锁（如CountDownLatch），
	  栅栏（如CyclicBarrier），
	  信号量（如Semaphore）
	  阻塞队列（如LinkedBlockingQueue）
    线程池：
     ThreadPoolExecutor
     Executors.newSingleThreadExecutor()
     Executors.newFixedThreadPool(10)
5.Comparable （方便，实体继承继承，不灵活）和 Comparator（灵活，多维度实现比较） 
6.Iterable（管理者，产生Iterator 去执行） 和 Iterator （具体操作者）

  
  